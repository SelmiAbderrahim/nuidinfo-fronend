import React from "react";

const index = ({ children }) => {
  return (
    <div>
      {/* Put navbar here */}
      {children}
      {/* Put navbar here */}
    </div>
  );
};

export default index;
